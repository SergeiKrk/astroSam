---
import Hero from '@/components/Hero'
import ListCategories from '@/components/ListCategories'
import ListPosts from '@/components/ListPosts'
import TitlePage from '@/components/TitlePage'
import BaseLayout from '@/layouts/BaseLayout'
import { getPosts } from '@/utils'
import { getEntryBySlug } from 'astro:content'
const homepage = await getEntryBySlug('calcPages', 'home')
const { title, description, headImg, titlePage, descriptionPage, urlSeo } = homepage.data

const MAX_POSTS = 5 // max number of posts to show on the home page
const posts = await getPosts(MAX_POSTS)
---

<BaseLayout title={title} description={description}>
	<Hero headImg={headImg} titlePage={titlePage} descriptionPage={descriptionPage} urlSeo={urlSeo} />
	<TitlePage title='Полезно знать' />
	<ListCategories />

	<div>
		<h2 class='text-lg font-medium tracking-wide text-end'>Новые статьи</h2>
		<ListPosts FirstBig={true} posts={posts} />
	</div>
</BaseLayout>

<script>
	import { animate } from 'motion'
	const showAnimations = localStorage.getItem('animations') === 'true'

	document.addEventListener('astro:page-load', () => {
		if (showAnimations) {
			animate(
				'.title',
				{ y: [80, 0], opacity: [0, 1] },
				{
					duration: 2.5,
					opacity: { duration: 3 },

					offset: [0, 0.55, 0.75]
				}
			)
		}
	})
</script>
